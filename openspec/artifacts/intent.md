# Intent

Prove that repository #7 consumes the immutable checkpoint contract produced by #1, verifies it before framework load and serves real CPU inference through a bounded FastAPI endpoint. Measure the public HTTP path with valid image bytes and expose Prometheus-compatible telemetry.

The primary number is successful requests per second; p95 HTTP latency and zero errors are supporting evidence. The checkpoint's low synthetic-fixture mAP limits the claim to delivery engineering, not domain accuracy.
